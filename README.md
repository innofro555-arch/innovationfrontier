# InnovationFrontier

## Usage

Install dependencies:

```bash
npm install
```

Run the application:

```bash
npm start
```

Check code style with ESLint:

```bash
npm run lint
```

Format files with Prettier:

```bash
npm run format
```
